<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <TextView android:text="@string/descLib" android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textView"
        android:layout_marginBottom="15dp"
        android:textSize="26sp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/inputColor"
        android:id="@+id/textInputColor"
        android:textSize="18sp"
        android:elevation="2dp"
        android:textColor="@color/primary_text_default_material_light"
        android:layout_below="@+id/textView"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="10dp"
        android:layout_marginRight="15dp" />

    <View
        android:layout_width="45dp"
        android:layout_height="30dp"
        android:id="@+id/colorInputView"
        android:elevation="3dp"
        android:clickable="true"
        android:padding="5dp"
        android:onClick="chooseColor"
        android:layout_alignTop="@+id/textInputColor"
        android:layout_toRightOf="@+id/textInputColor"
        android:layout_toEndOf="@+id/textInputColor"
        android:layout_marginLeft="10dp" />

</RelativeLayout>
